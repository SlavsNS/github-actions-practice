# Базовий образ
FROM node:lts

# Робоча директорія всередині контейнера
WORKDIR /app

# Копіюємо файли залежностей (кешування)
COPY package*.json ./

# Встановлюємо залежності
RUN npm install

# Копіюємо весь проєкт
COPY . .

# Відкриваємо порт
EXPOSE 3000

# Команда запуску
CMD ["node", "index.js"]